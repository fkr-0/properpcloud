# Public application metadata consumed by Android client builds.
PCLOUD_CLIENT_ID=

# Client builds intentionally do not consume PCLOUD_CLIENT_SECRET. Keep the
# secret only in an ignored local .env or an external secret manager for any
# future confidential/server-side tooling.
